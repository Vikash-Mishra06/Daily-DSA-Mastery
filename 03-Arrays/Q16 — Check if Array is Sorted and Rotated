// Q16 — Check if Array is Sorted and Rotated

    // function isSortedAndRotated(arr) {
    //   let count = 0;

    //   for (let i = 0; i < arr.length - 1; i++) {
    //     if (arr[i] > arr[i + 1]) {
    //       count++;
    //     }
    //   }

    //   // Compare last element with first element
    //   if (arr[arr.length - 1] > arr[0]) {
    //     count++;
    //   }

    //   return count <= 1;
    // }

    // console.log(isSortedAndRotated([3, 4, 5, 1, 2]));
